spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/snapsplit_test_db
    username: snapsplit_user
    password: snapsplit_password
  jpa:
    hibernate:
      ddl-auto: create-drop
  data:
    redis:
      host: localhost
      port: 6379

app:
  jwt:
    secret: test-secret-key-for-testing-only
    expiration-ms: 3600000
